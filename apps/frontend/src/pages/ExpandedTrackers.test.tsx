import{cleanup,fireEvent,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it,vi}from'vitest';import{CollectionsTracker,PerfectionTracker}from'./ExpandedTrackers';afterEach(cleanup);describe('expanded journal trackers',()=>{it('shows museum progress and keeps manual controls available',()=>{const put=vi.fn();render(<CollectionsTracker entries={[]} ownId={1} put={put}/>);expect(screen.getByText('Collections')).toBeTruthy();expect(screen.getByText((_,node)=>node?.tagName==='SPAN'&&node.textContent==='0 of 95')).toBeTruthy();fireEvent.click(screen.getByRole('button',{name:'Mark complete Dwarf Scroll I'}));expect(put).toHaveBeenCalledWith('shared','museum','96',{completed:true})});it('renders the weighted perfection dashboard',()=>{render(<PerfectionTracker entries={[]} members={[{id:1,displayName:'Farmer'}]}/>);expect(screen.getAllByText('0%').length).toBeGreaterThan(1);expect(screen.getByText('Produce & forage shipped')).toBeTruthy();expect(screen.getByText('Golden Clock')).toBeTruthy()})});
