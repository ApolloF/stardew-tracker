import{cleanup,fireEvent,render,screen}from'@testing-library/react';import{afterEach,describe,expect,it,vi}from'vitest';import{AnimalsTracker,CollectionsTracker,PerfectionTracker}from'./ExpandedTrackers';afterEach(cleanup);describe('expanded journal trackers',()=>{it('shows museum progress and keeps manual controls available',()=>{const put=vi.fn();render(<CollectionsTracker entries={[]} ownId={1} put={put}/>);expect(screen.getByText('Collections')).toBeTruthy();expect(screen.getByText((_,node)=>node?.tagName==='SPAN'&&node.textContent==='0 of 95')).toBeTruthy();fireEvent.click(screen.getByRole('button',{name:'Mark complete Dwarf Scroll I'}));expect(put).toHaveBeenCalledWith('shared','museum','96',{completed:true})});it('renders the weighted perfection dashboard',()=>{render(<PerfectionTracker entries={[]} members={[{id:1,displayName:'Farmer'}]}/>);expect(screen.getAllByText('0%').length).toBeGreaterThan(1);expect(screen.getByText('Produce & forage shipped')).toBeTruthy();expect(screen.getByText('Golden Clock')).toBeTruthy()})});
describe('imported tracker regressions',()=>{it('uses canonical sprites for white chickens and white cows',()=>{render(<AnimalsTracker entries={[{userId:null,scope:'shared',domain:'animals',itemId:'1',value:{name:'Kip',type:'White Chicken'}},{userId:null,scope:'shared',domain:'animals',itemId:'2',value:{name:'Eye',type:'White Cow'}}]} put={vi.fn()}/>);expect(screen.getByRole('img',{name:'White Chicken'}).getAttribute('src')).toBe('/game-icons/animals/chicken.webp');expect(screen.getByRole('img',{name:'White Cow'}).getAttribute('src')).toBe('/game-icons/animals/cow.webp')});it('uses the best single co-op farmer and converts total skill levels to farmer level',()=>{const entries:any[]=[{userId:1,scope:'player',domain:'shipping',itemId:'16',value:{completed:true}},{userId:2,scope:'player',domain:'shipping',itemId:'18',value:{completed:true}},...['farming','fishing','foraging','mining','combat'].map(itemId=>({userId:1,scope:'player',domain:'skills',itemId,value:{level:10}}))];render(<PerfectionTracker entries={entries} members={[{id:1,displayName:'Flo'},{id:2,displayName:'Princess'}]}/>);expect(screen.getByText((_,node)=>node?.tagName==='SPAN'&&node.textContent==='1 of 155')).toBeTruthy();expect(screen.getByText((_,node)=>node?.tagName==='SPAN'&&node.textContent==='25 of 25')).toBeTruthy()})});