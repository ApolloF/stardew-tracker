import{useState}from'react';
export default function GameIcon({src,label,size=36}:{src?:string;label?:string;size?:number}){const[failed,setFailed]=useState(false);if(!src||failed)return <svg className="game-icon-fallback" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" role={label?'img':undefined} aria-label={label||'Missing game icon'}><rect x="3" y="3" width="18" height="18" rx="3"/><path d="M7 16l3-3 2 2 3-4 3 5M8 8h.01"/></svg>;return <img className="game-icon" src={src} width={size} height={size} alt={label||''} aria-hidden={label?undefined:true} loading="lazy" onError={()=>setFailed(true)}/>}